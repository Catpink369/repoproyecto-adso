// RF2.1 a 2.5 - gestion de productos
const FRONT_URL = Cypress.env('FRONT_URL') || 'http://localhost:5173';

describe('RF-002.1 - ', () => {
    beforeEach(() => {
    });
});

describe('RF-002.2 - ', () => {
    beforeEach(() => {
    });
});

describe('RF-002.3 - ', () => {
    beforeEach(() => {
    });
});

describe('RF-002.4 - ', () => {
    beforeEach(() => {
    });
});

describe('RF-002.5 - ', () => {
    beforeEach(() => {
    });
});