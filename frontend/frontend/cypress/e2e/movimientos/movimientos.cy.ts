// RF3.1 a RF3.4
//const FRONT_URL = Cypress.env('FRONT_URL') || 'http://localhost:5173';

// Epica 3
describe('RF-003.1 - ', () => {
    beforeEach(() => {
    });
});

describe('RF-003.2 - ', () => {
    beforeEach(() => {
    });
});

describe('RF-003.3 - ', () => {
    beforeEach(() => {
    });
});

describe('RF-003.4 - ', () => {
    beforeEach(() => {
    });
});